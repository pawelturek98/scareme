{% extends 'admin/base.html.twig' %}

{% block title %}New Posts{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-2 col-sm-12 col-xs-12" style="text-align: center">
            <a href="{{ path('admin_posts_index') }}" class="btn btn-dark">back to list</a>
        </div>
        <div class="col-md-8 col-sm-12 col-xs-12">
            <div class="card">
                <div class="card-body">
                    <h1>Create new Posts</h1>
                    {{ include('admin/posts/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
