{% extends 'admin/base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-md-3 col-sm-3 col-xs-3 mb-3">
            <a href="{{ path('admin_contact_index') }}" class="btn btn-dark">back to list</a>
{#            <a href="{{ path('users_edit', {'id': user.id}) }}" class="btn btn-primary">Edit user</a>#}
{#            <button#}
{#                    class="btn btn-danger delete-item"#}
{#                    type="button"#}
{#                    data-delete_message="Are you sure you want to delete this user?"#}
{#                    data-delete_url="{{ path('users_delete', {'id': user.id}) }}">#}
{#                Delete#}
{#            </button>#}
        </div>
    </div>
    <div class="row">
        <div class="col-md-12 col-sm-12 col-xs-12">
            <div class="card">
                <div class="card-body">
                    <h3 class="cart-title">{{ contact.title }}</h3>
                    <table class="table table-bordered table-primary">
                        <tr>
                            <td>ID</td>
                            <td>{{ contact.id }}</td>
                        </tr>
                        <tr>
                            <td>First name</td>
                            <td>{{ contact.firstName }}</td>
                        </tr>
                        <tr>
                            <td>E-mail</td>
                            <td>{{ contact.email }}</td>
                        </tr>
                        <tr>
                            <td>Title</td>
                            <td>{{ contact.title }}</td>
                        </tr>
                        <tr>
                            <td>Content</td>
                            <td>{{ contact.content }}</td>
                        </tr>
                        <tr>
                            <td>Created at</td>
                            <td>{{ contact.createdAt | date('Y-m-d H:i:s') }}</td>
                        </tr>
                    </table>
                    <a href="mailto:{{ contact.email }}" class="btn btn-success">Respond</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}