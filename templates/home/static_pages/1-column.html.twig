{% extends 'base.html.twig' %}

{% block title %}{{ currentPage.title }}{% endblock %}
{% block seo_description %}{{ currentPage.seoDescription | escape }}{% endblock %}

{% block body %}
    <div class="container mb-5 mt-5">
        <div class="row">
            <div class="card">
                <div class="card-body">{{ currentPage.content | raw }}</div>
            </div>
        </div>
    </div>
{% endblock %}
